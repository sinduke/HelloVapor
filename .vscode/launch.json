{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Vapor App",
      "type": "swift",
      "request": "launch",
      "program": "${workspaceFolder}/.build/debug/App",
      "args": ["serve", "--env", "development"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
